
const initialState = {}

const sidebarReducer = (state: any =initialState, action: any) => {

    switch (action.type) {
        case "": {
            return state
        }
        default:
            return state
    }
}

export default sidebarReducer