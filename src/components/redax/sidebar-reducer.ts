const sidebarReducer = (state: any, action: any) => {

    switch (action.type) {
        case "": {
            return state
        }
        default:
            return state
    }
}

export default sidebarReducer